<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:sm="http://servicemix.org/config/1.0" 
	   xmlns:foo="http://servicemix.org/cheese/">

  <!-- lets run an embedded broker -->
  <bean id="broker" class="org.activemq.spring.BrokerFactoryBean">
    <property name="config" value="classpath:org/servicemix/components/jabber/broker.xml"/>
  </bean>

  <!-- the JBI container -->
  <sm:container id="jbi" depends-on="broker">
    <sm:activationSpecs>
      <sm:activationSpec componentName="myJabberSender" service="foo:myJabberSender">
        <sm:component><bean class="org.servicemix.components.jabber.JabberChatSender">
          <property name="host" value="localhost"/>
          <property name="port" value="6222"/>
          <property name="login" value="false"/>
          <property name="participant" value="test2@servicemix.org"/>
        </bean></sm:component>
      </sm:activationSpec>

      <sm:activationSpec componentName="myJabberReceiver" service="foo:myJabberReceiver" destinationService="foo:receiver">
        <sm:component><bean class="org.servicemix.components.jabber.JabberReceiver">
          <property name="host" value="localhost"/>
          <property name="port" value="6222"/>
          <property name="user" value="test2@servicemix.org"/>
          <property name="password" value="rocks"/>
        </bean></sm:component>
      </sm:activationSpec>

      <sm:activationSpec componentName="receiver" service="foo:receiver">
        <sm:component><bean class="org.servicemix.tck.ReceiverComponent"/></sm:component>
      </sm:activationSpec>
    </sm:activationSpecs>
  </sm:container>

  <bean id="client" class="org.servicemix.client.DefaultServiceMixClient">
    <constructor-arg ref="jbi"/>
  </bean>

</beans>
