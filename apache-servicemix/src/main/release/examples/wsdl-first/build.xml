<project name="jms-binding" default="setup" basedir=".">

  <target name="setup" description="Setup servicemix">
    <mkdir dir="data"/>
    <mkdir dir="install"/>
    <mkdir dir="deploy"/>
    <copy todir="install">
      <fileset dir="../../components" includes="*jsr181*,*http*"/>
    </copy>
    <copy todir="deploy">
      <fileset dir="sa/target" includes="*.jar"/>
    </copy>
  </target>

</project>
